import { createBrowserRouter } from "react-router-dom";

const  routerEndPoints = createBrowserRouter([{
    path:'/',
    element:<h1 className="text-3xl font-bold   underline">Hello World Main Route</h1>
}])

export default routerEndPoints